% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_doi_index.R
\name{build_doi_index}
\alias{build_doi_index}
\title{Build a Parquet DOI-lookup index}
\usage{
build_doi_index(
  root_dir = NULL,
  data_sets = "works",
  workers = NULL,
  memory_limit = NULL,
  temp_dir = NULL,
  batch_bytes = 8e+09,
  compression = "zstd",
  overwrite = FALSE,
  verbose = TRUE,
  corpus_dir = NULL
)
}
\arguments{
\item{root_dir}{Root directory containing a \verb{parquet/} subdirectory, or the
parquet directory itself.}

\item{data_sets}{Datasets to index. Only \code{works} carries a \code{doi} column, so
the default is \code{"works"}.}

\item{workers}{Number of parallel workers for Stage 1.}

\item{memory_limit}{DuckDB memory limit, e.g. \code{"20GB"}.}

\item{temp_dir}{Directory for Stage-1 shards and DuckDB spill. Stage 2 is a
single global sort of tens of GB and \emph{will} spill, so pointing this at a
fast local disk is the biggest speed lever available.}

\item{batch_bytes}{Approximate bytes of source parquet per Stage-1 batch.}

\item{compression}{Parquet compression codec.}

\item{overwrite}{Rebuild an existing index.}

\item{verbose}{Print progress.}

\item{corpus_dir}{Explicit path to a single dataset Parquet directory. When
given, \code{root_dir} and \code{data_sets} are ignored.}
}
\value{
Invisibly, the path to the index file (\code{corpus_dir} mode) or
\code{root_dir}.
}
\description{
Builds a \verb{<dataset>_doi_idx.parquet} index mapping normalised DOIs to
OpenAlex IDs and to their location in the Parquet corpus, so DOIs can be
resolved without network access.
}
\details{
The corpus stores DOIs as full lower-cased resolver URLs
(\verb{https://doi.org/10.1097/...}). The index key is the \strong{bare} DOI, so that
a caller may supply any of these and get the same answer:

\if{html}{\out{<div class="sourceCode">}}\preformatted{10.1016/j.joi.2017.08.007
https://doi.org/10.1016/j.joi.2017.08.007
http://dx.doi.org/10.1016/j.joi.2017.08.007
doi:10.1016/j.joi.2017.08.007
DOI: 10.1016/J.JOI.2017.08.007
}\if{html}{\out{</div>}}

Carrying \code{parquet_file} and \code{file_row_number} alongside the ID means
\code{\link[=lookup_by_doi]{lookup_by_doi()}} can extract full records from one index read rather than
two.

The index contains:
\describe{
\item{doi}{Normalised bare lower-case DOI, no resolver prefix}
\item{id}{OpenAlex ID in long form}
\item{id_block}{\code{floor(numeric_id / 10000)}, as in the ID index}
\item{parquet_file}{Path relative to the parquet root}
\item{file_row_number}{Row number within that file (0-indexed)}
}

Rows are sorted by \code{doi}, so a point lookup reads the footer plus one row
group rather than scanning the file.
}
\seealso{
\code{\link[=doi_to_id]{doi_to_id()}}, \code{\link[=lookup_by_doi]{lookup_by_doi()}}, \code{\link[=build_corpus_index]{build_corpus_index()}}
}
