% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/lookup_by_id.R
\name{lookup_by_id}
\alias{lookup_by_id}
\title{Look up records by OpenAlex ID}
\usage{
lookup_by_id(
  root_dir = NULL,
  ids,
  project_dir = NULL,
  data_sets = NULL,
  workers = NULL,
  progress = TRUE,
  verbose = TRUE,
  index_file = NULL,
  selected = NULL,
  output = NULL,
  backend = c("auto", "r", "rust"),
  columns = NULL,
  add_columns = NULL
)
}
\arguments{
\item{root_dir}{Root directory containing \verb{parquet/} and the dataset indexes
produced by \code{\link[=build_corpus_index]{build_corpus_index()}}. Index files are expected at
\verb{<root_dir>/parquet/<dataset>_id_idx.parquet}.}

\item{ids}{Character vector of OpenAlex IDs to retrieve. Can be long form
(e.g. \code{"https://openalex.org/W2741809807"}) or short form
(e.g. \code{"W2741809807"}).}

\item{project_dir}{Project output directory. Extracted Parquet files are
written to \verb{<project_dir>/snapshot_extract_<dataset>/}. Only used when
\code{root_dir} is provided.}

\item{data_sets}{Character vector of dataset names to search (e.g.
\code{c("works", "authors")}). \code{NULL} searches all indexed datasets under
\verb{<root_dir>/parquet/}. Ignored when \code{index_file} is provided.}

\item{workers}{Number of parallel workers for reading corpus files. Default
is \code{NULL} (sequential).}

\item{progress}{Ignored (kept for backward compatibility).}

\item{verbose}{Print progress messages. Default is \code{TRUE}.}

\item{index_file}{Explicit path to an index Parquet file created by
\code{\link[=build_corpus_index]{build_corpus_index()}}. When provided, \code{root_dir}, \code{data_sets}, and
\code{project_dir} are ignored.}

\item{selected}{Column selection passed to \code{arrow::open_dataset()}. Default
is \code{NULL} (all columns).}

\item{output}{Path to an output directory for writing results as Parquet
files when using \code{index_file} mode. If \code{NULL} (default), results are
returned as a data frame. Ignored when \code{root_dir} is used (use
\code{project_dir} instead).}

\item{backend}{Retained only so that existing calls passing
\code{backend = "rust"} get an explanatory error. The compiled backend was
removed in 0.1.0; the package is pure R. \code{"auto"} (the default) and
\code{"r"} both use the pure-R/DuckDB implementation. \code{"rust"} uses the
compiled library and is \strong{deprecated}: it writes an unsorted index and
supports neither \code{columns} nor \code{add_columns}. It will be removed in a
future release. \code{snapshot_to_parquet()} is unaffected and remains
Rust-only. \code{"auto"} (the default) uses the
compiled Rust library when it is loaded and the pure-R/DuckDB
implementation otherwise. \code{"r"} forces pure R and is always available.
\code{"rust"} forces the compiled path and errors if it is not loaded.}

\item{columns}{Character vector of columns to return. \code{NULL} (default)
returns all. Projection matters: reading 2 of 51 columns from a corpus of
nested structs is far cheaper than \verb{SELECT *}. Requires
\code{backend = "r"} — the compiled path always reads every column.}

\item{add_columns}{Named list of constant columns to add to every returned
row, e.g. \code{list(relation = "citing")}. Values are embedded as \strong{SQL string
literals}, matching \code{openalexPro::pro_request_parquet()}, so a logical
must be passed as \code{"TRUE"} and cast by the caller. Requires
\code{backend = "r"}.}
}
\value{
\itemize{
\item \code{index_file} mode, \code{output} not \code{NULL}: invisibly returns \code{output}.
\item \code{index_file} mode, \code{output} is \code{NULL}: returns a data frame of matching
records.
\item \code{root_dir} mode: invisibly returns \code{project_dir}.
}
}
\description{
Uses a pre-built index (created by \code{\link[=build_corpus_index]{build_corpus_index()}}) to locate records
efficiently and extract them from the Parquet corpus.
}
\details{
Paths can be supplied as a \code{root_dir} + \code{data_sets} pair (which
automatically locates the correct index files and writes output into
\code{project_dir}) or as an explicit \code{index_file} for direct use.
}
\examples{
\dontrun{
# root_dir mode (searches multiple datasets)
lookup_by_id(
  root_dir    = "/Volumes/openalex",
  ids         = c("W2741809807", "W1234567890"),
  project_dir = "my_project",
  data_sets   = "works"
)

# index_file mode (direct access, returns data frame)
records <- lookup_by_id(
  index_file = "works_id_index.parquet",
  ids        = c("W2741809807", "W1234567890")
)

# index_file mode (write to parquet)
lookup_by_id(
  index_file = "works_id_index.parquet",
  ids        = large_id_vector,
  output     = "filtered_works",
  workers    = 3
)
}

}
\seealso{
\code{\link[=build_corpus_index]{build_corpus_index()}} for building the required index,
\code{\link[=snapshot_to_parquet]{snapshot_to_parquet()}} for creating the Parquet corpus.
}
