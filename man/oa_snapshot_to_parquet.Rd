% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extendr-wrappers.R
\name{oa_snapshot_to_parquet}
\alias{oa_snapshot_to_parquet}
\title{Convert an OpenAlex snapshot to Parquet format.}
\usage{
oa_snapshot_to_parquet(
  snapshot_dir,
  parquet_dir,
  data_sets,
  workers,
  sample_size,
  memory_limit,
  temp_dir,
  verbose
)
}
\arguments{
\item{snapshot_dir}{Path to the snapshot root (contains a \verb{data/} subdir).}

\item{parquet_dir}{Output directory for Parquet files.}

\item{data_sets}{Character vector of dataset names, or \code{character(0)} for
all datasets found under \verb{snapshot_dir/data/} (excluding \code{merged_ids}).}

\item{workers}{Number of parallel workers (\code{1} = sequential).}

\item{sample_size}{Files to sample for schema inference (\code{0} = all).}

\item{memory_limit}{DuckDB memory limit, e.g. \code{"8GB"} (\code{""} = no limit).}

\item{temp_dir}{DuckDB temp directory (\code{""} = system default).}

\item{verbose}{Print progress to stderr.}
}
\value{
Invisibly returns \code{NULL}.
}
\description{
Full pipeline: schema inference (per-dataset, cached in
\verb{<parquet_dir>/<dataset>/.schema_cache/unified_schema.csv}) plus parallel
per-file COPY via rayon.
}
