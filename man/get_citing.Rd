% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_citing.R
\name{get_citing}
\alias{get_citing}
\alias{get_cited}
\title{Find the works citing, or cited by, given keypapers}
\usage{
get_citing(
  keypaper,
  root_dir = NULL,
  return = c("edges", "ids", "records"),
  output = NULL,
  columns = NULL,
  max_results = 100000L,
  depth = 1L,
  citation_index = NULL,
  id_index = NULL,
  doi_index = NULL,
  data_set = "works",
  workers = NULL,
  memory_limit = NULL,
  verbose = TRUE
)

get_cited(
  keypaper,
  root_dir = NULL,
  return = c("edges", "ids", "records"),
  output = NULL,
  columns = NULL,
  max_results = 100000L,
  depth = 1L,
  id_index = NULL,
  doi_index = NULL,
  data_set = "works",
  workers = NULL,
  memory_limit = NULL,
  verbose = TRUE
)
}
\arguments{
\item{keypaper}{Character \strong{vector} of one or more keypapers. Forms may be
mixed freely in a single call:
\itemize{
\item short OpenAlex ID, \code{"W3045921891"}
\item long OpenAlex ID, \code{"https://openalex.org/W3045921891"}
\item bare DOI, \code{"10.1016/j.joi.2017.08.007"}
\item DOI with resolver, \code{"https://doi.org/10.7717/peerj.4375"}
}

All keypapers are resolved and queried in one pass, not in a loop.}

\item{root_dir}{Root directory containing a \verb{parquet/} subdirectory, or the
parquet directory itself.}

\item{return}{What to return:
\itemize{
\item \code{"edges"} (default) -- a data frame of \code{from}/\code{to} long-form IDs, where
a row \verb{A, B} means \strong{A cites B}. This keeps the mapping between each
keypaper and its neighbours, which a flat ID vector would lose when
\code{keypaper} has length > 1, and it is directly \code{rbind}-able into a
snowball edge set.
\item \code{"ids"} -- the unique non-keypaper endpoint, as a character vector.
\item \code{"records"} -- the full records for those IDs, via \code{\link[=lookup_by_id]{lookup_by_id()}}.
}}

\item{output}{Directory to write records to when \code{return = "records"}. When
\code{NULL}, records are returned as a data frame.}

\item{columns}{Column projection passed to \code{\link[=lookup_by_id]{lookup_by_id()}} when
\code{return = "records"}.}

\item{max_results}{Error if more edges than this are found. A single heavily
cited work can have hundreds of thousands of citing works, and the
follow-on record extraction would then touch essentially every file in the
corpus. Set \code{Inf} to lift the guard deliberately.}

\item{depth}{Reserved for multi-hop snowballing; only \code{1} is implemented.}

\item{citation_index}{Explicit path to a \verb{*_cite_idx} directory.}

\item{id_index}{Explicit path to a \verb{*_id_idx.parquet}.}

\item{doi_index}{Explicit path to a \verb{*_doi_idx.parquet}.}

\item{data_set}{Dataset name used to locate indexes under \code{root_dir}.}

\item{workers}{Parallel workers for record extraction.}

\item{memory_limit}{DuckDB memory limit.}

\item{verbose}{Print progress.}
}
\value{
A data frame of edges, a character vector of IDs, or records --
see \code{return}.
}
\description{
\code{get_citing()} returns the works that \strong{cite} the keypapers; \code{get_cited()}
returns the works the keypapers \strong{reference}. Both work entirely offline
against a local snapshot.
}
\details{
The two are not symmetric in cost. \code{get_cited()} only needs the keypaper's
own \code{referenced_works} column, so it uses the ID index alone. \code{get_citing()}
asks the opposite question, which the snapshot does not answer directly --
\code{cited_by_api_url} is a URL -- so it needs the inverted index from
\code{\link[=build_citation_index]{build_citation_index()}}.
}
\seealso{
\code{\link[=build_citation_index]{build_citation_index()}}, \code{\link[=build_doi_index]{build_doi_index()}}, \code{\link[=lookup_by_id]{lookup_by_id()}}
}
